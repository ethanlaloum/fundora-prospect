# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Secrets
.env

# Sorties et cache runtime — jamais commitées : contiennent des donnees
# personnelles reelles issues du BODACC (cf. CLAUDE.md, contrainte 4).
out/
.cache/
explore/out/

# Base de l'entrepot. Elle vit hors du depot par defaut
# (~/.cache/fundora-prospect/prospects.db), mais FUNDORA_DB permet de la poser
# n'importe ou — y compris ici par commodite. Filet, pas garantie : la garantie
# est le defaut de `entrepot.chemin_base()`, verrouille par un test.
*.db
*.db-journal
*.db-wal

# Reglages Claude Code locaux (machine-specifiques)
.claude/settings.local.json

# OS / editeurs
.DS_Store
.idea/
.vscode/
